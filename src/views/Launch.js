import React, { Fragment } from "react";

// Launches view shows a list of launches.

const Launch = () => (
  <Fragment>
    <p>Single Launch should go here</p>
  </Fragment>
);

export default Launch;

